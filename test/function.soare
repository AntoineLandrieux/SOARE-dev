
@Fib(n)

    if n < 1 do
        return "Invalid Input"
    orif n equ 1 do
        return 0;
    orif n equ 2 do
        return 1;
    end;

    return Fib!(n-1) + Fib!(n-2);

end;

writeln Fib!(10);
